# Hero film v10

Same multi-contribution footage and timing as v9. Replaces the frozen, feathered table patch with a gradient-domain composite matched to each source frame. The phone core uses the same first-frame pixels at the same coordinates throughout. The surrounding table blends into each frame’s illumination instead of retaining an old patch of light.

1600 × 1066, 24 fps, 145 frames, silent H.264 with faststart. Poster extracted from encoded first frame.
