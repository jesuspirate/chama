# v6.5 implementation and release readiness

This is an implementation checkpoint, not a release declaration. No deployment,
live claim, wallet replacement, event-kind migration, or flag flip is authorized
by passing local tests alone. Original brief: `v6.5-brief.md`.

## Rotation preconditions — all five must be true

| Precondition | Evidence/status | Satisfied? |
| --- | --- | --- |
| 1. Replay blocker closed | Diagnostics, strict money-event replay, recorded advisory skips and explicit-open backfill implemented. The specific trade has no LOCK in the measured community-relay response; buyer decryption/claim has not been demonstrated. Runway 32 records the finding and required recovery evidence. | No |
| 2. Relay retention answered | Measured: community relay retains collision; three default public relays replace it. Raw results and separate migration brief checked in. Retention risk is established, not repaired. | Diagnostic answered; production risk open |
| 3. Quiet sign-in and native push shipped together | Quiet-window policy, Android receivers/registration and VPS transport support implemented. Firebase configuration, VPS rollout and real device delivery remain unverified. No release has shipped. | No |
| 4. Reader fleet compatibility | No fleet-version inventory or minimum compatible adoption measurement available. | No |
| 5. Multiple-device full cycle | Existing simulation tests are not evidence of a completed multi-device run with host locking last and a collection round. No such device record is available. | No |

`CHAMA_RING_WRITER_ENABLED` and `CHAMA_ROTATION_ENABLED` remain **false**.
Before enabling either, attach evidence for every row, publish cycle-failure and
sacrifice-round copy in en/es/fr/sw, and verify both flags change in the same
release. First public circles must remain small. Do not substitute an engine
unit test for a device or fleet observation.

## Native push — implemented, deployment and device acceptance open

The Android shell now has a Capacitor push plugin, UnifiedPush connector and FCM
service. Background alerts remain opt-in. Play services plus Firebase configuration
select FCM; otherwise an installed UnifiedPush distributor can be selected through
the system picker. Settings name the transport. No Firebase token is requested
before opt-in. Disabling removes local interests, unregisters the endpoint and
suppresses delivery immediately even when network cleanup fails.

The native store holds only opaque watch tags and endpoint capabilities. Token
rotation re-registers existing interests; foreground resume retries registration.
The VPS supports encrypted UnifiedPush payloads and FCM data-only messages with a
120-second TTL. It independently rejects old, future and pre-registration events,
deduplicates relay deliveries, and permits only trusted push endpoint hosts. Server
credentials never enter the app. Registration uses possession of the opaque endpoint
capability, as the existing Web Push path does; it does not attach a Nostr identity.

A native wake says only “There may be new activity. Open Chama to check.” It never
asserts a payment or outcome, and never opens or operates a wallet in the background.
Opening the app still fetches and verifies escrow state. Foreground native wakes
are suppressed so live notification policy owns that session. This preserves the
existing opaque-wake architecture; it is not a headless signer implementation.

Before deployment, configure the Firebase Android application for `app.chama.market`,
provide its public `android/app/google-services.json` at build time, and configure
`FCM_SERVICE_ACCOUNT_FILE` on the VPS. Existing FCM-less builds remain usable with
UnifiedPush. See `scripts/vps-webpush-watcher/README.md` for endpoint hosts and rollout.
Neither external configuration nor deployment has been performed in this change.

Acceptance still requires a physical no-Play-services device/distributor and a
Play-services device, app closed, with real fresh transitions and historical
replay controls. The user confirmed no existing test setup they could identify.
Nothing here is represented as native push shipped.

## Record and text rules

Arbiter records describe signed conduct in the history this device has observed.
No history means unknown/new, never good. Bonds are verified funds at risk, not
proof of identity or independence. This copy rule is mandatory for trade,
dashboard and roster renderings.

Listing bodies are bounded at 8,000 characters at the wire boundary. React text
nodes render pasted HTML literally. The parser exposes no HTML or image nodes;
HTTP(S) links show their destinations and use noopener/noreferrer/nofollow.
Legacy readers continue to see `description` as the short title.

Avatar uploads are embedded raster only, capped at 48 KiB and 2048 × 2048.
Locally decoded first-frame PNGs provide a reduced-motion fallback. Third-party
picture URLs are not fetched. Profile publishing merges the latest readable
kind-0 document, preserving unrelated fields.

## Rail audit

Browse category results, Browse shelf results, goods canvas matches and exchange
canvas matches use `groupBySettlementRail` / `settlementRailOf`, preserving rank
inside groups and naming mixed groups. Existing guided tests cover ecash defaults,
on-chain-only headings, mixed rails and unknown rails. The guided payment-method
question uses the same payment-rail registry as review labels; those fiat payment
methods are distinct from escrow settlement and must not be relabelled as an
on-chain/ecash choice.

## Validation limits

A screenshot sweep of every authenticated screen, a two-device listing/avatar
round trip, real buyer recovery, native push delivery and fleet readiness remain
external acceptance checks. Local parser/replay tests exercise the wire/body
round trip and malicious text, but do not substitute for those device checks.

## Local verification completed

- `npm run predeploy`: hygiene, TypeScript and full regression suite passed;
  escrow engine reports 4,170 passed, zero failed. New watcher transport tests
  pass in the standard test command.
- Final avatar and arbiter-record review tightened static-image validation and
  excluded future votes from concentration. Both affected suites and TypeScript
  were rerun successfully after those changes.
- `npm run build` passed after the final source changes. Existing Vite chunk-size
  and mixed-import warnings remain; no build error.
- Android `:app:compileDebugJavaWithJavac` and `:app:testDebugUnitTest` passed with
  JDK 21; both native policy tests passed. This is not physical push-delivery proof.
- Actual component fixtures were inspected at desktop and 390px mobile widths:
  the single attention pill, hostile listing text, and empty arbiter record.
  The fixture is reproducible with `scripts/v65-ui-review.ts`; this is not a full
  authenticated screen sweep.
- `git diff --check` passed. Existing user edits and the supplied brief were kept.

No application release, VPS deployment, signed money operation, or protocol kind/
`d`-tag migration was performed. The evidence gaps in the five-row table above
remain release gates, not tasks that local green checks can waive.
